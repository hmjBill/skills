---
name: env-bootstrap
description: "开发环境引导与依赖补齐，安装缺失工具链并验证环境可用性"
---

# 环境引导

触发场景：

- 用户要求安装缺失工具链（如 LSP、CLI、MCP 依赖）。
- 用户要求验证全局环境是否可用。

执行原则：

1. 先确认当前状态（版本、路径、命令可执行性）。
2. 仅安装明确缺失项。
3. 安装后立即复检并报告结果。
4. 配置修改必须可追溯（文件路径 + 变更点）。

约束：

- 不执行破坏性命令。
- 不隐式修改用户业务代码仓库。
- 不跳过验证步骤。
